name: Discover HK/SG price API (temp)
on:
  push:
    branches: [claude/hk-price-api-check]

jobs:
  discover:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/discover-hk-price.mjs
