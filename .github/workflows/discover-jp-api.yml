name: Discover JP price API (temp)
on:
  push:
    branches: [claude/jp-price-api-probe]

jobs:
  discover:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/discover-jp-xml.mjs
