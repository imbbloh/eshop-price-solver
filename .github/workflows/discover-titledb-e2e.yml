name: Discover titledb e2e (temp)
on:
  push:
    branches: [claude/titledb-e2e-check]

jobs:
  discover:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/discover-titledb-e2e.mjs
